metro_files+=([atuin]="$DOTS/atuin/config.toml")
metro_files+=([bright]="$bin_dir/brightness-manager")
metro_files+=([cava]="$DOTS/cava/config")
metro_files+=([ff]="$DOTS/fastfetch/config.jsonc")
metro_files+=([gammaconf]="$DOTS/gammastep/config.ini")
metro_files+=([git]="$DOTS/lazygit/config.yml")
metro_files+=([gitig]="$HOME/.gitignore")
metro_files+=([hbinds]="$HYPR/keybinds.conf")
metro_files+=([hcolors]="$HYPR/colors.conf")
metro_files+=([hconf]="$HYPR/hyprland.conf")
metro_files+=([hdecor]="$HYPR/decoration.conf")
metro_files+=([hidle]="$HYPR/hypridle.conf")
metro_files+=([hlock]="$HYPR/hyprlock.conf")
metro_files+=([hrules]="$HYPR/rules.conf")
metro_files+=([imv]="$DOTS/imv/config")
metro_files+=([kcolors]="$DOTS/kitty/colors.conf")
metro_files+=([kconf]="$DOTS/kitty/kitty.conf")
metro_files+=([keyd]="/etc/keyd/default.conf")
metro_files+=([mime]="$HOME/.config/mimeapps.list")
metro_files+=([mise]="$DOTS/mise/config.toml")
metro_files+=([mkconf]="/home/x8u/.config/mako/config")
metro_files+=([p10k]="$HOME/.p10k.zsh")
metro_files+=([pyreconf]="$bin_dir/pyre")
metro_files+=([pyrefn]="$HOME/.config/pyre/functions.sh")
metro_files+=([rfcolors]="$DOTS/rofi/colors.rasi")
metro_files+=([rfconf]="$DOTS/rofi/config.rasi")
metro_files+=([rfstyle]="$DOTS/rofi/style.rasi")
metro_files+=([ryzen]="/usr/local/sbin/apply-ryzen-settings.sh")
metro_files+=([ssh]="$HOME/.ssh/config")
metro_files+=([topgrade]="$DOTS/topgrade.toml")
metro_files+=([uair]="$DOTS/uair/uair.toml")
metro_files+=([userjs]="${zen_profile}/user.js")
metro_files+=([wbcolors]="$DOTS/waybar/colors.css")
metro_files+=([wbconf]="$DOTS/waybar/config")
metro_files+=([wbstyle]="$DOTS/waybar/style.css")
metro_files+=([zals]="$ZSH_CONFIG_DIR/50_aliases.zsh")
metro_files+=([zdot]="$ZSH_CONFIG_DIR/lib/dotfiles.zsh")
metro_files+=([zenv]="$ZSH_CONFIG_DIR/10_environment.zsh")
metro_files+=([zfn]="$ZSH_CONFIG_DIR/60_functions.zsh")
metro_files+=([zfzf]="$ZSH_CONFIG_DIR/40_fzf.zsh")
metro_files+=([zinit]="$ZSH_CONFIG_DIR/70_init.zsh")
metro_files+=([zmetro]="$ZSH_CONFIG_DIR/lib/metro.zsh")
metro_files+=([zopts]="$ZSH_CONFIG_DIR/20_options.zsh")
metro_files+=([zplugs]="$ZSH_CONFIG_DIR/30_plugins.zsh")
metro_files+=([zrc]="$HOME/.zshrc")
metro_files+=([zreg]="$ZSH_CONFIG_DIR/lib/registry.zsh")
metro_files+=([zregd]="$ZSH_CONFIG_DIR/lib/registry.data.zsh")
metro_files+=([ztools]="$ZSH_CONFIG_DIR/lib/tools.zsh")
metro_modules+=([hypr]="$HYPR")
metro_modules+=([kitty]="$DOTS/kitty")
metro_modules+=([nvim]="$DOTS/nvim/lua/")
metro_modules+=([pyre]="$DOTS/pyre")
metro_modules+=([rofi]="$DOTS/rofi")
metro_modules+=([scripts]="$bin_dir")
metro_modules+=([waybar]="$DOTS/waybar")
metro_modules+=([z]="$ZSH_CONFIG_DIR")
metro_modules+=([zapps]="$ZSH_CONFIG_DIR/apps")
metro_modules+=([zlib]="$ZSH_CONFIG_DIR/lib")
